mod root;
mod nav_bar;
mod hero;
mod page_header;
mod button;
mod footer;

pub type Footer = footer::Footer;

pub type NavBar = nav_bar::NavBar;
pub type NavBarProps = nav_bar::NavBarProps;

pub type Hero = hero::Hero;

pub type PageHeader = page_header::PageHeader;
pub type PageHeaderProps = page_header::PageHeaderProps;

pub type Root = root::Root;
